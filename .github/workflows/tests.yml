name: Tests

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  integration-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Start WordPress environment
        run: npm run wp:start

      - name: Run integration tests
        run: npm run test:integration

      - name: Stop WordPress environment
        if: always()
        run: npm run wp:stop

  astro-build-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Start WordPress environment
        run: npm run wp:start

      - name: Run Astro build integration tests
        run: npm run test:build

      - name: Stop WordPress environment
        if: always()
        run: npm run wp:stop
